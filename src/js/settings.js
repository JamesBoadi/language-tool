/* =============================================================================
                                Internal Settings
   ========================================================================== */
/**
 * These settings are controlled by the developer,
 * the settings directly affect the behaviour
 * of the application based on the languageTool API
 * 
 * * */

var protocol = "https://"; // http://

var endpoint = "api.languagetool.org"; // localhost:8081

var enabledOnly = false;

var highlightColor = "#FFFF00;"; // Change the background color of text with errors

/**
 * ***************************
 * Inactive settings
 * ***************************
 * 
 * * */

var username = "";

var apiKey = "";

var dicts = ""

var motherTongue = "";

var preferredVariants = "";

var enabledRules = "";

var disabledRules = "";

var enabledCategories = "";

var disabledCatergories = "";


